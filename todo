PROJECT CONTEXT PROMPT (PAINTINGS / DIGITAL MUSEUM APP)

I am building a full-stack Digital Museum / Paintings Gallery web app.

🧠 STACK
Frontend
React + TypeScript (Vite)
Chakra UI
Framer Motion
React Router
Backend
Node.js + Express
MongoDB Atlas (Mongoose)
Winston logging (structured logging system implemented)
🏛 CURRENT ARCHITECTURE
API
GET /api/paintings → fetch all paintings
GET /api/paintings/:id → fetch single painting (with related paintings populated)
Static Images
Served via Express:
/paintings → maps to local image folder
📦 DATA MODEL (Mongoose)

Painting schema:

title
artist
year
medium
description
imageUrls (array)
tags (array)
relatedPaintings (ObjectId refs → self-referencing via populate)
🎨 CURRENT DATASET

We have 24 paintings total seeded into MongoDB:

Original 8 paintings:
The Death of Socrates
Reply of the Zaporozhian Cossacks
Water from the Rock
The Bard
The Chess Players
Daniel in the Lion's Den
The Knight in the Enchanted Garden
The Knight of the White Cross
Added 16 classical/masterworks:

Includes works from:

Neoclassicism (David, Ingres)
Romanticism (Delacroix, Géricault, Friedrich, Fuseli)
Symbolism (Klimt, Böcklin, Millais, Goya)
Renaissance (Michelangelo, Leonardo)
Baroque / Realism (Rembrandt, Repin)
Replacement made:
“Saturn Devouring His Son” was replaced with:
→ “The Sleep of Reason Produces Monsters” (Goya)
🔗 RELATED PAINTINGS SYSTEM (CURRENT STATE)
relatedPaintings is an array of MongoDB ObjectIds
backend uses .populate("relatedPaintings")
frontend displays related works in PaintingDetails page
system is NOT fully connected yet (graph not built)
🎯 FRONTEND STATE
Pages implemented:
Landing page (animated)
Gallery page (grid + filters + search frontend-only)
Painting details page (dynamic route /painting/:id)
Painting Details page:
Fetches single painting from backend
Displays:
image
title, artist, year, medium
description
related paintings section (basic UI, now needs proper graph logic)
⚙️ BACKEND IMPROVEMENTS DONE
Logging system upgraded:
Winston logger implemented
request logging middleware added
structured logs with metadata
logs saved to:
logs/combined.log
logs/error.log
replaced console.log usage in controllers
🚧 NEXT DEVELOPMENT PRIORITIES
Build relatedPaintings graph system
manual + intelligent linking based on:
tags
art movement
artist
themes
Improve Painting Details page:
better UI polish
related carousel / grid upgrade
Backend search/filter (move logic from frontend → backend)
Optional future scaling:
recommendation system
museum-style navigation experience
🧭 PROJECT GOAL

Build a high-quality digital museum experience with:

curated art structure (not random CRUD)
meaningful relationships between paintings
smooth UI animations
scalable backend architecture
portfolio-grade code quality
🚀 START CONTEXT

When continuing, assume:

backend + frontend already fully working
24 paintings already seeded
logging system already implemented
we are now focusing on data relationships + UX improvements

📦 DATA MODEL (Mongoose)

Painting schema includes:

title
artist
year
medium
description
imageUrls (array)
tags (array)
relatedPaintings (ObjectId refs to other paintings)
🎨 DATASET

24 total paintings seeded:

Classical + Romanticism + Renaissance + Symbolism + Baroque + Realism
Includes Goya, Klimt, Delacroix, Rembrandt, Michelangelo, Leonardo, etc.
“Saturn Devouring His Son” replaced with “The Sleep of Reason Produces Monsters”
🧠 GRAPH SYSTEM (IMPORTANT FEATURE)

We built a relationship engine:

buildRelationships.js
compares every painting with every other painting
computes similarity score using:
same artist (+5)
shared art movement (+4 per match)
shared tags (+2 per tag)
keyword overlap in description
stores top related paintings in MongoDB (relatedPaintings array)
📊 GRAPH EXPORT SYSTEM
exportGraph.js
converts MongoDB paintings into:
{
  nodes: [{ id, title, artist }],
  links: [{ source, target }]
}
outputs graph.json used by frontend visualization
🌐 FRONTEND STATE

Pages:

Landing page (animated)
Gallery page (search + grid)
Painting details page (shows related paintings)
Graph page (/graph) → force-directed graph visualization
🧠 GRAPH VISUALIZATION

We use:

react-force-graph-2d

Features:

nodes = paintings
links = relationships
click node → navigate to painting details
colored clusters by artist
interactive physics simulation

Current improvements:

improved node visibility
link styling
force simulation tuning
labels rendered via canvas

================
===== TODO =====
================

# PROTECTED ROUTES
# WRITE UI TESTS FOR AUTHENTICATION (JEST OR RTL)
# FIX FRONTEND PAINTING DETAILS PAGE -> MANUALLY FIX IMAGE QUALITY WITH BETTER PHOTOS
# ADD MULTIPLE LANGUAGE TRANSLATION
# UPDATE GITHUB README WITH RELATABLE IMAGES
  -> winston logging of REST requests, nodemon scripts, package.json
  -> graph build and export scripts + UI view

# Create backend for authentication with:
    [DONE] # Server-side logging
    # Unit Testing
    # UML diagrams
    [DONE] # MongoDB database 
    # Rate Limiting and security